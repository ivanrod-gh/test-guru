<p><%= t('.header', email: @resource.email) %></p>

<p><%= t('.unblock_instructions') %></p>

<p><%= t('.to_unlock') %></p>

<p><%= link_to t('.unblock_account'), unlock_url(@resource, unlock_token: @token) %></p>
