<p><%= t('.header', email: @resource.email) %></p>

<p><%= t('.password_reset_requested') %></p>

<p><%= link_to t('.change_password'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('.ignore_email') %></p>
<p><%= t('.changed_after_action') %></p>
