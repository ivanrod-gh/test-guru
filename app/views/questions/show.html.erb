<h2>Тест "<%= Test.find(params[:test_id]).title %>", тело вопроса с id "<%= @question.id %>":</h2>
<p style="color: Red"><%= @question.body %></p>
<br>

<%= button_to('Удалить вопрос', '', method: "delete", data: { confirm: 'Вы уверены?' }) %>