<h2><%= answer_header(@answer) %> <%= t('.answer_for') %>:</h2>
<div style="border: 1px solid lightgray; padding: 5px;">
  <%= @answer.question.body %>
</div>

<%= form_with model: [:admin, @question, @answer], local: true do |form| %>
  <%= render 'shared/errors', instance: @answer %>

  <p>
    <div><%= form.label :body, t('.body') %>:</div>
    <div><%= form.text_area :body, id: :answer_body, cols: 45, rows: 5 %></div>
  </p>

  <p>
    <%= form.check_box :correct, id: :answer_correct %>
    <%= form.label :correct, t('.correct') %>
  </p>

  <br><p><div><%= form.submit %></div></p>
<% end %>
